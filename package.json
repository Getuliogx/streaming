{
  "name": "minha-stream-simples",
  "version": "9.0.0",
  "private": true,
  "description": "Site de streaming simples com importador universal de playlists por link, filmes e séries separados, categorias, gêneros, A-Z e TMDB.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/assets/app.js && node --check public/assets/watch.js && node --check public/assets/admin.js && node --check public/assets/series.js"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "compression": "^1.7.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "express-session": "^1.18.1",
    "helmet": "^8.0.0"
  }
}
